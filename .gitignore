node_modules
.next
.next-dev

# Секреты. На сервере боевой файл — .env (его читает деплой),
# локально обычно .env.local. Под игнор попадают оба и все варианты,
# кроме .env.example, который специально отслеживается.
.env
.env.*
!.env.example

# logs
*.log
dev-out.log
dev-err.log

# build cache
tsconfig.tsbuildinfo

# temp artifacts
temporary screenshots/

# python
__pycache__/
*.pyc

# исходники для publish_photos.py (на сайт уезжают копии в public/images/portfolio)
NewPhoto/
